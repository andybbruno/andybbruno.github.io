---
import { getCollection } from "astro:content";
import { siteConfig } from "../config";

const posts = (await getCollection("blog")).sort(
  (a, b) => a.data.order - b.data.order,
);
const hasPosts = posts.length > 0;
---

{
  hasPosts && (
    <section id="blog" class="p-8 sm:p-12 md:p-16 lg:p-24">
      <div>
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
          <div class="lg:col-span-4">
            <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-gray-900">
              Blog
            </h2>
            <div
              class="w-[75px] h-[5px] mt-2 rounded-full"
              style={`background-color: ${siteConfig.accentColor}`}
            />
          </div>

          <div class="lg:col-span-8">
            <div class="space-y-8">
              {posts.map((post) => (
                <a
                  href={`/blog/${post.data.slug}`}
                  class="group block p-4 sm:p-6 md:p-8 bg-gray-50 rounded-xl sm:rounded-2xl border border-gray-200 transition-all duration-300 hover:shadow-xl hover:border-gray-300 hover:-translate-y-1"
                >
                  <div class="space-y-4">
                    <h3 class="text-xl sm:text-2xl font-bold text-gray-900 pr-8">
                      {post.data.title}
                    </h3>

                    <p class="text-base sm:text-lg text-gray-600 leading-relaxed">
                      {post.data.description}
                    </p>

                    <span
                      class="inline-flex items-center gap-1 text-sm sm:text-base font-medium transition-all duration-300 group-hover:gap-2"
                      style={`color: ${siteConfig.accentColor}`}
                    >
                      Read article
                      <span aria-hidden="true">→</span>
                    </span>
                  </div>
                </a>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
